// import axios from 'axios'

const state = {
  title: 'Hello World'
};
const getters = {
  getTitle: (state) => state.title
};
const actions = {};
const mutations = {};

export default {
  state,
  getters,
  actions,
  mutations
};